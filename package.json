{
  "name": "fh-salzburg",
  "version": "1.0.0",
  "description": "Slides for Lectures at FHS",
  "main": "index.js",
  "scripts": {
    "postinstall": "npm install -g reveal-md",
    "start": "reveal-md . --css styles.css",
    "build": "rm -rf build && npm run build:html && npm run build:pdf",
    "build:html": "npm run start -- --static",
    "build:pdf": "npm run start -- --print build/slides.pdf",
    "deploy": "npm run build:html && gh-pages -d _static && rm -rf _static"
  },
  "author": "Thomas Mayrhofer",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^2.0.1"
  },
  "dependencies": {
    "reveal-md": "^3.0.0-1"
  }
}
